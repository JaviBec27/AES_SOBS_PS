import { SideBarDragableDirective } from './sidebar.directive';

describe('SideBarDragableDirective', () => {
  it('should create an instance', () => {
    const directive = new SideBarDragableDirective();
    expect(directive).toBeTruthy();
  });
});
